export function LoginPage() {
  // Stub: should not be shown when AuthProvider always returns a user
  return (
    <div className="flex min-h-dvh items-center justify-center px-6">
      <p className="text-muted-foreground">ログイン画面（未実装）</p>
    </div>
  )
}
